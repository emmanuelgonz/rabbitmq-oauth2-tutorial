version: '3'

services:
  nginx-proxy:
    image: nginxproxy/nginx-proxy
    container_name: nginx-proxy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
      - ./data/certs:/etc/nginx/certs:ro
      - ./data/vhost.d:/etc/nginx/vhost.d
      - ./data/html:/usr/share/nginx/html
    restart: always

  acme-companion:
    image: nginxproxy/acme-companion
    container_name: acme-companion
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./data/certs:/etc/nginx/certs:rw
    environment:
      DEFAULT_EMAIL: emmanuelgonzalez@asu.edu
    depends_on:
      - nginx-proxy
    restart: always
    volumes_from:
      - nginx-proxy

  # keycloak:
  #   image: quay.io/keycloak/keycloak:20.0
  #   networks:
  #     - rabbitmq_net
  #   ports:
  #     - "8080:8080"
  #     - "8443:8443"
  #   environment:
  #     KEYCLOAK_ADMIN: admin
  #     KEYCLOAK_ADMIN_PASSWORD: admin
  #   volumes:
  #     - ./conf/keycloak/import:/opt/keycloak/data/import
  #     - ./certs:/etc/letsencrypt
  #   command: start-dev --import-realm --https-certificate-file=/etc/letsencrypt/live/nost.smce.nasa.gov/fullchain.pem --https-certificate-key-file=/etc/letsencrypt/live/nost.smce.nasa.gov/privkey.pem
  #   restart: always
